# Requirements for the HF Spaces (ZeroGPU) deployment, where the live-upload
# TRIBE path actually runs. The base requirements.txt stays torch-free for the
# no-GPU / local viewer; this file is the superset the Space installs.
#
# On the Space, set the hardware to ZeroGPU. `spaces` is provided by the Space
# runtime; `tribev2` is installed from source.

gradio>=4.44
numpy>=1.26
opencv-python-headless>=4.9
librosa>=0.10
imageio-ffmpeg>=0.5
soundfile>=0.12
scikit-learn>=1.4
joblib>=1.3

# GPU inference stack
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.3
pandas>=2.2
nilearn>=0.10

# TRIBE v2 (inference). Installed from the Meta research repo. CC-BY-NC.
tribev2 @ git+https://github.com/facebookresearch/tribev2

# ZeroGPU decorator (no-op off-Spaces; the app degrades gracefully without it)
spaces
