# --- App + audiovisual engine — CPU only, no torch. Runs locally & on Spaces. ---
gradio>=4.44
numpy>=1.26
opencv-python-headless>=4.9   # video frame analysis (motion, cuts, detail)
librosa>=0.10                 # audio loudness / onset analysis
imageio-ffmpeg>=0.5           # bundled ffmpeg for audio decode (no system install)
soundfile>=0.12
scikit-learn>=1.4             # Ridge engagement model (train + serve)
joblib>=1.3                   # load the trained model artifact

# --- Dataset collection (run locally to build a training set) ---
# yt-dlp>=2024.8              # pip install yt-dlp; used by pipeline/collect.py

# --- TRIBE path (optional, GPU) — installed only on the HF Space. ---
# See requirements-space.txt. Kept out of here so the base env stays torch-free.
#   torch, tribev2, spaces, nilearn, pandas, yt-dlp

# --- Dev ---
pytest>=8.0
